[project]
name = "fetcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arrow>=1.4.0",
    "dekeyrej-secretmanager>=1.1.5",
    "httpx[http2]>=0.28.1",
    "orjson>=3.11.9",
    "pyyaml>=6.0.3",
    "redis>=8.0.0",
]
