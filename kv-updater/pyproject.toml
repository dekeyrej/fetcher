[project]
name = "kv-updater"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "orjson>=3.11.9",
    "redis>=8.0.0",
]

[tool.uv.sources]
redis-lib = { path = "../redis_lib" }
